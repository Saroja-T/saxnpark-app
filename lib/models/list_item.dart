class ListItem {
  final String icon;
  final String label;
  final double width;
  final bool isSelected;

  ListItem({
    required this.icon,
    required this.label,
    required this.width,
    required this.isSelected,
  });
}
